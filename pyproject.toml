[project]
name = "ai-config-review-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "paramiko",
    "ssh-audit",
    "pytest",
    "pypdf>=6.14.2",
    "google-genai",
    "python-dotenv",
]
